# Abyss Walker Configuration
# width and height are in maze cells (min 3, max 30x20)
# seed: 0 = random each run, any other number = fixed generation
# density: 100 = perfect maze (no extra paths), 0 = very open (many shortcuts)

width=30
height=20
seed=0
density=100
